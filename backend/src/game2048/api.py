"""HTTP API layer for game2048 (placeholder).

Deferred per the current task scope (engine only). This module will expose
endpoints (e.g. create a game, query the board, apply a move) and the
request/response models, delegating all logic to :mod:`game2048.engine`.
"""

"""AI move suggestion (placeholder).

Deferred per the current task scope (engine only). This module will host an
offline heuristic move evaluator that, given a board, suggests the best move
(``'left'`` / ``'right'`` / ``'up'`` / ``'down'``) to avoid game over and
maximise the chance of winning. No remote services or credentials will be used.
"""

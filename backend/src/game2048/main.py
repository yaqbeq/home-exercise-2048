"""Application entrypoint (placeholder).

Deferred per the current task scope (engine only). This module will create the
web application, wire up the routes from :mod:`game2048.api.routes`, and expose
a runnable server (e.g. ``uvicorn game2048.main:app``).
"""
